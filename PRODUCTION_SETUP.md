# 🚀 EventSphere — Стан продакшну та запуску

> Оновлено: 2026-07-29
> Стан: **сайт живий і відкритий для користувачів** ✅ · БД + авторизація працюють · бронювання у режимі вільного доступу (без оплати)

---

## 📍 Поточний стан (перевірено на проді)

| Що | Значення |
|----|----------|
| **GitHub репо** | `RomanVolchovskiy/eventsphere` (гілка `main`, public) |
| **Vercel команда / проєкт** | `rpman's projects` / `eventsphere` |
| **Прод-URL** | https://eventsphere-rpmans-projects.vercel.app |
| **Автодеплой** | ✅ кожен `git push` у `main` автоматично збирає й публікує |
| **База даних (Neon + Prisma)** | 🟢 працює — перевірено реєстрацією й сторінкою виконавця |
| **Каталог `/catalog`** | 🟢 читає виконавців з БД, фільтр за категоріями працює (виправлено 2026-08-04) |
| **Smart Match** | 🟢 підбирає з БД; движок у `src/lib/match.ts`, rate limit 30/10хв за IP (виправлено 2026-08-04) |
| **Планувальник `/planner`** | 🟢 показує заходи авторизованого користувача з БД (виправлено 2026-08-04) |
| **`/daily`** | 🚧 демо — заклади захардкоджені, відповідників у схемі немає; на сторінці є банер про це |
| **Авторизація (email)** | 🟢 реєстрація і вхід перевірені на проді 2026-07-29 |
| **Авторизація (Google)** | 🟢 працює — перевірено живим входом 2026-07-30 |
| **Бронювання** | 🟢 режим вільного доступу — підтверджується одразу, без оплати |
| **Платежі (Stripe)** | ⏸️ вимкнені (Stripe не працює для UA — див. нижче) |

**Висновок:** сайт уже публічно доступний. Користувачі можуть реєструватися (email/Google),
переглядати каталог і **бронювати безкоштовно**. Запускати `vercel deploy` вручну НЕ треба.

> ⚠️ Локальний `.vercel/project.json` вказує на старий проєкт (інша команда). Це не заважає,
> бо деплой іде через GitHub-інтеграцію, а не з локальної машини.

---

## ✅ Що вже зроблено (кроки 1–2 завершені)

Раніше ці кроки були в планах — зараз вони **виконані й перевірені**:

- **env-змінні у Vercel** виставлені: `DATABASE_URL`, `NEXTAUTH_URL`, `NEXTAUTH_SECRET`,
  `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`.
- **БД (Neon)** підключена через Vercel → Storage.
- **Авторизація email** — реєстрація і вхід перевірені на живому сайті.

Перевірити самостійно: відкрити сайт → `/register` → зареєструватися новою поштою.

> ⚠️ **Не перевіряйте авторизацію через `/api/auth/providers`.** Ця сторінка показує провайдера
> у списку навіть із вигаданим ключем — вона підтверджує лише те, що провайдер увімкнено в коді.
> Саме через таку «перевірку» в цьому файлі роками стояло, що Google-вхід працює, хоча
> в `GOOGLE_CLIENT_ID` лежав заповнювач. Єдина справжня перевірка — пройти вхід руками.
>
> ℹ️ **`/catalog` тепер справді читає БД** (з 2026-08-04) — сторінка динамічна (`ƒ` у виводі
> `next build`), тож порожній каталог означає порожню або недоступну базу. До того список був
> зашитий масивом і виглядав однаково навіть при мертвій БД — саме через це в цьому файлі
> тривалий час стояло, що «БД працює, каталог віддає дані».

### 🔍 Хардкод замість даних — перевірено 2026-08-04

Сайт будувався інтерфейсом уперед, тож кілька розділів жили на вигаданих масивах. Спільна
прикмета: виконавці з id `"1"`…`"6"`, тоді як у БД ключі — cuid, тож посилання вели у 404.
Знайдено й виправлено: `/catalog`, `/api/smart-match`, `/planner`. Перевірено й визнано
здоровими: `/dashboard` (реальні дані + авторизація), `/pro` (масиви — це тарифи, їм там місце).
`/daily` лишається демонстраційним із банером.

**Як перевіряти надалі:** 200 і правдоподібний вигляд нічого не доводять. Дивитись, чи є в
сторінці виклик `getDb()`, і чи відкриваються посилання, які вона генерує.

---

## 💳 Платежі — режим вільного доступу (поточний)

**Чому без оплати:** Stripe офіційно **не підтримує акаунти, зареєстровані в Україні**
для прийому платежів. Тому на період набору користувачів платний checkout вимкнено,
а бронювання підтверджується одразу.

**Як це керується — один прапорець у Vercel env vars:**

| `NEXT_PUBLIC_PAYMENTS_ENABLED` | Поведінка бронювання |
|-------------------------------|----------------------|
| не встановлено / ≠ `true` (зараз) | **Вільний доступ** — бронювання `CONFIRMED` одразу, без оплати |
| `=true` | Платний Stripe Checkout (потрібні Stripe-ключі, див. нижче) |

> Після зміни прапорця обов'язково **Redeploy** — env підхоплюються лише новим деплоєм.

### Коли будуть платежі: варіанти для України

Stripe для UA-юрособи не підходить. Реальні шляхи:

1. **Український платіжний шлюз** (рекомендовано для UA-ринку) — підтримують ФОП/ТОВ, UAH,
   локальні картки, Apple/Google Pay:
   - **WayForPay** або **Fondy** — підтримують *split-платежі* (комісія платформі + виплата
     виконавцю), що ідеально для маркетплейсу
   - **LiqPay** (ПриватБанк) — найпростіший старт для ФОП
   - **monobank acquiring / Plata by mono** — сучасний, швидке підключення
2. **Закордонна юрособа** (Польща / Естонія e-Residency / US LLC через Stripe Atlas) — тоді
   можна лишити Stripe, але додається юридична/податкова складність.

Заміна торкнеться 3 файлів: `src/app/api/payments/checkout/route.ts`,
`src/app/api/payments/webhook/route.ts`, `src/lib/stripe.ts` + клієнтський виклик у
`BookingPanel.tsx`. Логіка бронювань лишається.

### (Довідково) Якщо вмикати Stripe

1. Ключі: https://dashboard.stripe.com/apikeys → `STRIPE_SECRET_KEY`,
   `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`
2. Вебхук: Developers → Webhooks → endpoint
   `https://<домен>/api/payments/webhook`, події `checkout.session.completed`,
   `charge.refunded` → `STRIPE_WEBHOOK_SECRET`
3. Додати ці 3 ключі + `NEXT_PUBLIC_PAYMENTS_ENABLED=true` у Vercel → Redeploy
4. Тест-картка: `4242 4242 4242 4242`, будь-яка майбутня дата, будь-який CVC

---

## 🔐 Безпека — зробити найближчим часом

- [x] ✅ **БД перестворено** (2026-07-29) — стару базу покинуто разом зі скомпрометованим паролем.
  Нова база створена через **Vercel → Storage** (Neon), тож `DATABASE_URL` тепер керується
  інтеграцією й доступна з того ж акаунта Vercel. Схему розгорнуто `prisma migrate deploy`,
  дані залито `prisma/seed.ts`. Стара база лишилась в іншому, недоступному акаунті Neon.
- [x] ✅ **`NEXTAUTH_SECRET` ротовано** (2026-07-29) — попередня версія цього файлу містила його
  у відкритому вигляді в **публічному** репозиторії. Новий секрет виставлено у Vercel (Production)
  і в локальному `.env`, зроблено Redeploy; перевірено — `/api/auth/session` порожня.
- [x] ✅ **Демо-акаунти без паролів** (2026-07-30) — `prisma/seed.ts` лежить у публічному репо,
  тож пароль `password123` з нього знав будь-хто. Тестового клієнта `test@eventsphere.com`
  видалено з БД і з seed; п'ятьом демо-виконавцям `passwordHash` очищено (`null`) і прибрано
  з seed. Вхід ними тепер відхиляється (`CredentialsSignin`) — перевірено на проді.
  Картки каталогу від цього не залежать: `Vendor` посилається на `User`, а не на пароль.
- [x] `.env*` не комітиться (вже в `.gitignore`)
- [x] HTTPS активний (Vercel автоматично)
- [ ] Старий секрет Google (закінчується на `9G8U`) — невживаний, видалити в Credentials
- [ ] При вмиканні платежів — Live-ключі, не Test

---

## КРОК (опційно) — Власний домен

1. Vercel → проєкт → **Settings → Domains** → **Add**
2. Введіть домен → додайте DNS-записи у реєстратора (Vercel покаже які)
3. Дочекайтесь видачі SSL (HTTPS автоматично)
4. **Оновіть** `NEXTAUTH_URL` на новий домен
5. **Додайте** домен у Google OAuth (origins + redirect URI); за наявності платежів — у webhook URL
6. Redeploy

### Google OAuth — де що лежить

Діючий клієнт (налаштований 2026-07-30):

| | |
|---|---|
| Google Cloud проєкт | `eventsphere-498808` |
| OAuth-клієнт | **Web client 1** (Web application) |
| Publishing status | `In production` — заходити можуть усі, верифікація не потрібна |
| Client ID | `299144292043-33a5b4f180ibvet0ga0kliu1uli3hqo5.apps.googleusercontent.com` |

Прописані адреси — **обидві пари обов'язкові**:
- origins: `https://eventsphere-rpmans-projects.vercel.app` + `http://localhost:3000`
- redirect URIs: ті самі, з хвостом `/api/auth/callback/google`

Секрет у консолі видно **лише в момент створення**. Якщо його немає під рукою — не шукайте,
а випустіть новий: Credentials → клієнт → Client secrets → **Add secret**. Старий працює
паралельно, доки його не видалити.

Перевірити ключ, не заходячи в акаунт (покаже `redirect_uri_mismatch`, якщо адреса не прописана):
```
curl -s -i "https://accounts.google.com/o/oauth2/v2/auth?client_id=<CLIENT_ID>&redirect_uri=<URLENCODED_CALLBACK>&response_type=code&scope=openid%20email%20profile" | grep -i ^location
```

При зміні домену: додати нові origins + redirect URI в цьому ж клієнті, оновити `NEXTAUTH_URL`, Redeploy.

> Доступ до Google Calendar API у вході **не запитується** (лише `openid email profile`) — саме тому
> верифікація застосунку не потрібна. Якщо колись знадобиться календар, scope додається окремо
> й тягне за собою верифікацію.

---

## ⚙️ Технічна примітка: БД і rate limiting

Єдине джерело істини для БД — **Prisma + Neon** (`DATABASE_URL`). Supabase-клієнт
(`src/lib/supabase.ts`) прибрано разом із залежністю `@supabase/supabase-js`, щоб не
тримати два паралельні шляхи до даних.

Rate limiting — власний in-memory лімітер (`src/lib/ratelimit.ts`), лічильник живе в межах
одного serverless-інстансу. Цього досить проти простого зловживання; для масштабу знадобиться
спільне сховище (Redis).

---

## 🔗 Корисні посилання

| Сервіс | URL |
|--------|-----|
| Vercel (проєкт) | https://vercel.com/rpmans-projects/eventsphere |
| Vercel env vars | https://vercel.com/rpmans-projects/eventsphere/settings/environment-variables |
| Прод-сайт | https://eventsphere-rpmans-projects.vercel.app |
| Neon Database | https://console.neon.tech |
| Google Cloud | https://console.cloud.google.com |

---

## 🆘 Troubleshooting

| Симптом | Причина / рішення |
|---------|-------------------|
| Реєстрація/каталог падає | Перевірити `DATABASE_URL` і зробити Redeploy |
| `Configuration` error на логіні | `NEXTAUTH_SECRET` / `NEXTAUTH_URL` не збігається з доменом |
| `redirect_uri_mismatch` (Google) | Redirect URI у Google ≠ домен сайту (має точно збігатися) |
| Бронювання просить оплату, хоча має бути безкоштовним | Прибрати `NEXT_PUBLIC_PAYMENTS_ENABLED` (або ≠ `true`) → Redeploy |
| Зміни в env не діють | Env підхоплюються лише новим деплоєм — зробіть Redeploy |
